# snagify

Snagify is a CLI to debug works-on-my-machine issues by detecting environment drift.

Install:

    pipx install snagify

Run:

    snagify check

This PyPI package installs/runs the prebuilt Snagify binary from GitHub Releases. It does not require Go.
