# snagify

Snagify is a CLI to debug works-on-my-machine issues by detecting environment drift.

Install:

    npm install -g snagify

Run:

    snagify check

This npm package installs the prebuilt Snagify binary from GitHub Releases. It does not require Go.
